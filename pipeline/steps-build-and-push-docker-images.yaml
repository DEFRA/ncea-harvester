steps:

- task: Docker@2
  displayName: 'Docker Build'
  condition: and(succeeded(), eq(variables.isPullRequest, false))
  inputs:
    command: build
    containerRegistry: $(acrContainerRegistry)
    repository: $(containerRespository)
    tags: '$(imageTag)'

- task: Docker@2
  displayName: 'Docker Push'
  condition: and(succeeded(), eq(variables.isPullRequest, false))
  inputs:
    command: push
    containerRegistry: $(acrContainerRegistry)
    repository: $(containerRespository)
    tags: '$(imageTag)'