apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "ncea-harvester.fullname" . }}-test-connection"
  labels:
    {{- include "ncea-harvester.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "ncea-harvester.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
