apiVersion: v1
kind: Service
metadata:
  name: {{ include "ncea-harvester.fullname" . }}
  labels:
    {{- include "ncea-harvester.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "ncea-harvester.selectorLabels" . | nindent 4 }}
